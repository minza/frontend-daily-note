js里的函数声明分3部分
1进入环境，向环境声明变量(declare)
2初始化(initial)
3赋值(assignment)


var声明的变量会在进入环境后的的创建变量后会立马初始化为unifined，所以在赋值前可以访问（undifined）
let,const,class进入环境后会创建变量，但是不会立马初始化，而是在第一次let的时候初始化为unifined或者传入的值，这就造成了暂时性死区（temporal dead zone）