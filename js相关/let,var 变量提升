var的变量提升：预编译阶段会把var的声明提到作用域的顶部，并赋值为'undefined'
let其实也存在变量提升，会在吧声明提升到作用域顶部，（但是不会赋值），所以直到let 申明的时候才会初始化，如果没有赋值的话则是'undefined',这也是'暂时性死区'

引用es6翻译过来的一段话:
由let,const创建的变量，会在包含环境所在的词法环境实例化时被创建，但只有变量的词法绑定求值运算后才能被访问

暂时性死区其实是为了限制在定义之前就是用变量，这样会导致一些不熟悉var原理的同学以为可以在var声明之前就使用变量，这样会埋下隐患