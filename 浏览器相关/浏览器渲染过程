过程：
解析html生成dom树
解析css生产cssom树
将dom和css合并成render树
回流（重排）,根据计算节点的大小位置等集合属性，应用到render树上
重绘，根据回流得到的几何信息计算绝对像素
将像素发送给GPU，显示在桌面上


具体：生成render树的过程
从dom的跟节点遍历每一个可见节点
找到节点对于的cssom树上的规则，并应用
根据dom和css规则，生成render树


回流的具体过程
render树得到了节点的具体集合信息，可是还需要知道视口大小和确切位置，这个过程就叫回流

重绘的具体过程
根据那些节点是可见的，以及他们的样式转化为屏幕上的具体的像素点，这个过程就叫做重绘
